// Compatibility export. The visual-bounds contract is shared by Layout and
// Canvas Groups so collapsed/Nodes 2.0 geometry cannot drift between features.
export { resolveNodeVisualBounds } from "../core/node-visual-bounds.js";
